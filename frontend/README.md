# VulnSeeker Frontend

React + Vite + TypeScript frontend for the VulnSeeker web console.

Use the project-level startup guide: [../WEB_STARTUP.md](../WEB_STARTUP.md).

```powershell
npm install
npm run dev
```
